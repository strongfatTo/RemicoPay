{
  "name": "remicopay-contracts",
  "version": "1.0.0",
  "description": "RemicoPay smart contracts on Etherlink",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network etherlinkTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
